[versions]
# Defino las versiones de las herramientas principales de desarrollo
agp = "8.13.2"        # Versión del plugin de Android para Gradle
kotlin = "2.0.21"     # Versión del lenguaje Kotlin que estoy usando
ksp = "2.0.21-1.0.25" # Herramienta para procesar las anotaciones de Room de forma rápida
room = "2.6.1"        # Versión de la base de datos Room
coreKtx = "1.17.0"    # Extensiones esenciales de Android para Kotlin
junit = "4.13.2"      # Versión para las pruebas unitarias
junitVersion = "1.3.0"
espressoCore = "3.7.0"
lifecycleRuntimeKtx = "2.10.0" # Gestión del ciclo de vida (ViewModels, Corrutinas)
activityCompose = "1.12.1"     # Integración de Actividades con Jetpack Compose
composeBom = "2024.09.00"      # "Bill of Materials" para que todas las librerías de Compose sean compatibles entre sí
roomCommonJvm = "2.8.4"
material3 = "1.4.0"            # Versión de los componentes visuales de Material Design 3

[libraries]
# Aquí catalogo cada librería con un nombre corto para usarlo en los archivos build.gradle
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycleRuntimeKtx" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }

# Configuro el paquete de Compose para asegurar que la UI sea moderna y reactiva
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }

# Registro las librerías necesarias para que Room funcione correctamente
androidx-room-common-jvm = { group = "androidx.room", name = "room-common-jvm", version.ref = "roomCommonJvm" }
androidx-material3 = { group = "androidx.compose.material3", name = "material3", version.ref = "material3" }

[plugins]
# Defino los plugins que activan las capacidades de Android, Kotlin y el procesamiento de Room
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }